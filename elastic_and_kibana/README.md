### how to run

1. install requirments via pip 
2. docker up ```docker compose up -d```
3. docker down ```docker compose down -v```
4. python setup envirnment with requirments
5. follow the verifay minio steps
6. generate_and_upload.py for data creation and query_lake.py for query demo
7. for schema demo python build_schema_lake.py an than compare_query_times.py


2️⃣ Verify MinIO is up

Open browser:

👉 http://localhost:9001

Login:

User: minio

Password: minio123

Create a bucket called:

lake

Create a bucket called:

schemalake


